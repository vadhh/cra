# Daily Engineering Report — CRA (Consolidated)

**Period:** 2026-07-20 → 2026-07-22 (WIB)
**Author:** Afridho (Engineering Owner)
**Branch:** `release/cra-1.0-rc1`
**Commits:** `83c0f61` → `b8c2100` → `a86297a` → `56d1082` → `21f780d` → `34d965c` → `7845851` → `6f78b63` → `54e0ea4` → `4bbc744` → `2c51e7e`
**Test baseline (end of period):** ✅ 108 / 108 passed, 3 warnings (pre-existing, harmless third-party SWIG/tokenizer warnings)

---

## 1. Executive Summary

Three-day arc closing the reviewer's entire 2026-07-17 punch list (6 items) plus all 4 follow-ups it surfaced. No merge to production occurred at any point; `release/cra-1.0-rc1` remains unmerged, and the controlled-pilot-only readiness verdict is unchanged throughout.

- **07-20**: Corrected report terminology (0/56 legal approval, not 11/56; smoke tests ≠ corpus validation). Fixed `datetime.utcnow()` deprecation. Built the original-11 corpus validation harness and ran it for the first time: 27/33 target fixtures, 4 real misclassification bugs found.
- **07-21**: Closed the 4th corpus bug (31/33, 0 known bugs). Restricted the pilot UI to the 11 original profiles, server-enforced on both entry points. Ran all 13 documented classifier collision pairs for the first time (baseline: A-signal 7/13, B-signal 11/13). This closed all 6 items of the 07-17 punch list.
- **07-22**: Fixed 3 orphaned-label bugs (`it_services_contract`, plus `construction_contract`/`insurance_contract` found along the way), fixed 2 misclassification double-misses (`outsourcing_agreement`, `maintenance_contract` → `employment_contract`), and closed the last 2 fixture-content gaps (`commercial_agreement`, `partnership_agreement`). Original-11 corpus harness reached **33/33 target fixtures, 0 gaps, 0 known bugs**.

End state: original-11 corpus fully green on every objectively-checkable dimension (identification, dangerous-clause detection, recommendation wiring, PDF generation). Risk-score accuracy remains PENDING — still blocked on Ilham's lawyer-reviewed ground truth, deliberately not asserted to avoid repeating the metric-inflation pattern the reviewer flagged on 07-17.

---

## 2. Status Progression by Dimension

| Dimension | 07-20 | 07-21 | 07-22 |
| :--- | :--- | :--- | :--- |
| **Original-11 Corpus Validation** | 27/33 fixtures, 4 known bugs | 31/33 fixtures, 0 known bugs | **33/33 fixtures, 0 gaps, 0 known bugs** |
| — Identification / dangerous-clause / recommendation / PDF | 27/27 each | 31/31 each | **33/33 each** |
| — Risk score accuracy | PENDING | PENDING | PENDING (unchanged — blocked on Ilham) |
| **Manual Contract-Type Selection** | Unrestricted (5 legacy UI options) | Restricted to 11 profiles, server-enforced | Unchanged |
| **13 Collision-Pair Coverage** | Documented only, untested | A-signal 7/13, B-signal 11/13 | A-signal **9/13**, B-signal 11/13 (2 orphaned-label + 1 double-miss fix moved A-signal) |
| **Legal Approval / Formal Review** | 0/56 | 0/56 | 0/56 (unchanged all 3 days) |
| **Public Production Readiness** | NOT READY | NOT READY | NOT READY (unchanged — controlled pilot only) |

Readiness percentage estimates (engineering architecture 75–80%, original-11 technical implementation 80–85%, controlled-pilot readiness 65–70%, public release 55–60%, complete 56-profile product 35–40%, formal legal validation 0–10%) are the reviewer's 07-17 figures, carried forward unchanged across all three days — none of this work moved readiness itself; it closed engineering punch-list items.

---

## 3. Changes by Day

### 07-20
- **`datetime.utcnow()` deprecation fix** — 3 call sites (`database.py`, `manage.py`, `test_sprint4.py`) switched to `datetime.now(timezone.utc)`; warnings dropped 26 → 3.
- **Original-11 corpus harness built** ([run_original11_corpus.py](file:///home/stardhoom/LDV/ldv-backend/tests/run_original11_corpus.py)) — first real (non-smoke) end-to-end execution of fixtures through L1–L3 + PDF export for the 11 validated profiles. Found 6 fixture gaps and 4 real misclassification bugs (French employment, Dutch lease, Dutch NDA, and — most concerning — an abusive-clause injection flipping the *detected document type*, not just the score).

### 07-21
- **4th misclassification bug fixed** — `bench_purchase_agreement_neg.txt` (NLI confidently but wrongly picked a `draft`-status label over a strong keyword match on a `validated` profile). Added a narrow 4th override condition to `classify_document_type()`: trust keyword match over NLI when NLI's pick is `draft`-status and keyword's pick is `validated`-status.
- **Pilot restricted to 11 profiles** — `PILOT_TYPE_MAPPING` / `_resolve_pilot_type()` gated on registry `classifier.status == "validated"`; enforced with early 400-rejection on both `POST /api/v1/upload?type=...` and `POST /api/v1/result/<id>/retry`. Frontend dropdowns expanded from 5 legacy options to the real 11 (they had never exposed more than 5 to begin with).
- **13 collision pairs tested for the first time** ([run_collision_pairs.py](file:///home/stardhoom/LDV/ldv-backend/tests/run_collision_pairs.py)) — baseline A-signal 7/13, B-signal 11/13. Found 5 expected draft→validated collisions, 2 unexplained double-misses (`maintenance_contract`, `outsourcing_agreement` → `employment_contract`), and 1 distinct orphaned-label bug (`it_services_contract`). None are silent-wrong-result risks in the pilot — `worker._needs_confirmation()`'s draft-profile gate already forces human confirmation regardless.

### 07-22
- **`it_services_contract` orphaned label fixed** — hardcoded keyword chain returned a legacy pre-consolidation string (`"it service agreement"`) that never matched the registry's actual display name. Fixed 3 occurrences to `"it services contract"`.
- **`outsourcing_agreement` → `employment_contract` fixed** — plain substring match on `"employee"` false-triggered inside `"employees"` in a title-matching branch. A broad fix (word-boundary-match all 18 branches) regressed a different case and was reverted; scoped to the single `employment` branch instead.
- **`maintenance_contract` → `employment_contract` fixed**, decided without needing the flagged "joint override-logic decision" — root cause was simply a missing title-match branch (same precedented pattern as `construction`/`insurance`), not new override logic. Auditing the chain surfaced 2 more identical orphaned-label bugs (`construction_contract`, `insurance_contract`), fixed alongside it. First attempt at a bare substring match regressed 4 unrelated collision pairs via boilerplate text; rescoped to the registry's exact alias phrase, word-boundary matched.
- **2 fixture-content gaps closed** (`commercial_agreement`, `partnership_agreement`) — both already had an unused `docx/*_pos.docx` variant sitting in the fixtures directory (same content, different container, matching the precedent already accepted for `purchase_agreement`'s 3rd slot) that had never been wired into the harness's `FIXTURE_MAP`. Verified both classify correctly before wiring in.

---

## 4. Ilham's Action Items (Unchanged Throughout, Principal Bottleneck)

Carried across all three days without engineering-side progress possible:

1. Expected-results matrix for the 33 corpus fixtures (risk score, dangerous-clause severity, recommendation wording) — harness is ready to consume this once delivered.
2. Reconciliation of the 45-partial-profiles vs. 42-detection-specifications discrepancy (3 profiles unexplained).
3. Profile-by-profile legal validation matrix with named reviewer, review date, and approval status per profile.
4. Joint CRA Release Gate Matrix (5 gates: engineering regression / corpus validation / security validation / legal review / controlled-pilot acceptance).
5. Judgment call on the 5 confirmed keyword-duplication collisions (surfaced 07-21) — which profile should "own" each disputed keyword.

---

## 5. End-of-Period State (2026-07-22 EOD)

No open engineering items from this thread. All 6 items of the 07-17 punch list closed (07-21), all 4 follow-ups from that closure closed (07-22). Remaining blockers are entirely non-engineering: legal sign-off, collision-pair keyword-ownership decisions, and risk-score ground truth — all on Ilham/reviewer side, tracked in §4 above and in `docs/legal_review_packet.md` (Gate 4).

---

## 6. Sign-off

- **Engineering Owner:** Afridho (Engineering Owner)
- **Status:** SUBMITTED FOR REVIEW
- **Period:** 2026-07-20 through 2026-07-22
