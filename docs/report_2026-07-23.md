# Daily Engineering Report — CRA (Consolidated)

**Period:** 2026-07-20 → 2026-07-23 (WIB)
**Author:** Afridho (Engineering Owner)
**Branches:** `release/cra-1.0-rc1` and `staging` (merged and kept in sync as of 07-23 — see §7)
**Commits (RC1, 07-20→07-22):** `83c0f61` → `b8c2100` → `a86297a` → `56d1082` → `21f780d` → `34d965c` → `7845851` → `6f78b63` → `54e0ea4` → `4bbc744` → `2c51e7e`
**Commits (07-23):** `89e970b` (gate matrix + 45v42 reconciliation) → `b9a1bd3` (merge `release/cra-1.0-rc1` → `staging`, pulling in Ilham's parallel work) → `de7c7d9` (fix dead duplicate `_NLI_OVERRIDE_THRESHOLD`) → `dc1623a` (resolve 45v42 using Ilham's data) → `2d37cbc` (sync `required_clauses` for 3 pending profiles)
**Test baseline (end of period):** ✅ 108 / 108 passed, 3 warnings (pre-existing, harmless third-party SWIG/tokenizer warnings)

---

## 1. Executive Summary

Four-day arc closing the reviewer's entire 2026-07-17 punch list (6 items), all 4 follow-ups it surfaced, and — new as of 07-23 — the joint CRA Release Gate Matrix plus a first real merge with Ilham's parallel documentation/data stream. No merge to production occurred at any point; `release/cra-1.0-rc1` remains unmerged to `master`, and the controlled-pilot-only readiness verdict is unchanged throughout.

- **07-20**: Corrected report terminology (0/56 legal approval, not 11/56; smoke tests ≠ corpus validation). Fixed `datetime.utcnow()` deprecation. Built the original-11 corpus validation harness and ran it for the first time: 27/33 target fixtures, 4 real misclassification bugs found.
- **07-21**: Closed the 4th corpus bug (31/33, 0 known bugs). Restricted the pilot UI to the 11 original profiles, server-enforced on both entry points. Ran all 13 documented classifier collision pairs for the first time (baseline: A-signal 7/13, B-signal 11/13). This closed all 6 items of the 07-17 punch list.
- **07-22**: Fixed 3 orphaned-label bugs (`it_services_contract`, plus `construction_contract`/`insurance_contract` found along the way), fixed 2 misclassification double-misses (`outsourcing_agreement`, `maintenance_contract` → `employment_contract`), and closed the last 2 fixture-content gaps (`commercial_agreement`, `partnership_agreement`). Original-11 corpus harness reached **33/33 target fixtures, 0 gaps, 0 known bugs**.
- **07-23**: Built the joint 5-gate CRA Release Gate Matrix (1/5 passed at time of writing). Discovered and merged **~270 files / ~20,000 lines of previously-unseen parallel work already pushed to `origin/staging`** by Ilham — legal evidence docs, detection specifications, a gold-standard validation dataset, corpus expected results. Fixed a pre-existing dead-code bug uncovered during the merge (`_NLI_OVERRIDE_THRESHOLD` silently shadowed 0.85 → 0.40). Used Ilham's `GOLD_STANDARD_VALIDATION_SUMMARY.md` to close the long-standing 45-vs-42 detection-spec discrepancy, then synced `required_clauses` for the 3 profiles that discrepancy traced back to. Full detail in §7.

End state: original-11 corpus fully green on every objectively-checkable dimension (identification, dangerous-clause detection, recommendation wiring, PDF generation). Risk-score accuracy remains PENDING — still blocked on Ilham's lawyer-reviewed ground truth, deliberately not asserted to avoid repeating the metric-inflation pattern the reviewer flagged on 07-17. 5 of the 13 classifier collision pairs are also still unresolved (down from 6 — see §7).

---

## 2. Status Progression by Dimension

| Dimension | 07-20 | 07-21 | 07-22 | 07-23 |
| :--- | :--- | :--- | :--- | :--- |
| **Original-11 Corpus Validation** | 27/33 fixtures, 4 known bugs | 31/33 fixtures, 0 known bugs | **33/33 fixtures, 0 gaps, 0 known bugs** | Unchanged |
| — Identification / dangerous-clause / recommendation / PDF | 27/27 each | 31/31 each | **33/33 each** | Unchanged |
| — Risk score accuracy | PENDING | PENDING | PENDING (unchanged — blocked on Ilham) | PENDING (unchanged) |
| **Manual Contract-Type Selection** | Unrestricted (5 legacy UI options) | Restricted to 11 profiles, server-enforced | Unchanged | Unchanged |
| **13 Collision-Pair Coverage** | Documented only, untested | A-signal 7/13, B-signal 11/13 | A-signal **9/13**, B-signal 11/13 (2 orphaned-label + 1 double-miss fix moved A-signal) | **1 more pair closed by Ilham** (`joint_venture_agreement` vs `partnership_agreement`) — 5/13 pairs now unresolved |
| **45-vs-42 Detection-Spec Discrepancy** | Open, unexplained | Open | Open | **✅ RESOLVED** using Ilham's `GOLD_STANDARD_VALIDATION_SUMMARY.md`: 42 "Partial" + 3 "Pending Engineering Sync" = 45; all 45/45 have a spec |
| **required_clauses Sync (3 pending profiles)** | N/A (issue not yet identified) | N/A | N/A | **✅ DONE** — registry ↔ legacy JSON reconciled for `construction_contract`/`insurance_contract`/`it_services_contract` |
| **CRA Release Gate Matrix** | Did not exist | Did not exist | Did not exist | **Created** — 1/5 gates passed (Engineering Regression); Gate 2 substantially advanced today |
| **Legal Approval / Formal Review** | 0/56 | 0/56 | 0/56 (unchanged all 3 days) | 0/56 (unchanged — Ilham's merged docs confirm this explicitly, see §7) |
| **Public Production Readiness** | NOT READY | NOT READY | NOT READY (unchanged — controlled pilot only) | NOT READY (unchanged) |

Readiness percentage estimates (engineering architecture 75–80%, original-11 technical implementation 80–85%, controlled-pilot readiness 65–70%, public release 55–60%, complete 56-profile product 35–40%, formal legal validation 0–10%) are the reviewer's 07-17 figures, carried forward unchanged across all four days — none of this work moved readiness itself; it closed engineering punch-list items and reconciled two previously-separate reporting streams (Afridho's and Ilham's) into one gate matrix.

---

## 3. Changes by Day

### 07-20
- **`datetime.utcnow()` deprecation fix** — 3 call sites (`database.py`, `manage.py`, `test_sprint4.py`) switched to `datetime.now(timezone.utc)`; warnings dropped 26 → 3.
- **Original-11 corpus harness built** ([run_original11_corpus.py](file:///home/stardhoom/LDV/ldv-backend/tests/run_original11_corpus.py)) — first real (non-smoke) end-to-end execution of fixtures through L1–L3 + PDF export for the 11 validated profiles. Found 6 fixture gaps and 4 real misclassification bugs (French employment, Dutch lease, Dutch NDA, and — most concerning — an abusive-clause injection flipping the *detected document type*, not just the score).

### 07-21
- **4th misclassification bug fixed** — `bench_purchase_agreement_neg.txt` (NLI confidently but wrongly picked a `draft`-status label over a strong keyword match on a `validated` profile). Added a narrow 4th override condition to `classify_document_type()`: trust keyword match over NLI when NLI's pick is `draft`-status and keyword's pick is `validated`-status.
- **Pilot restricted to 11 profiles** — `PILOT_TYPE_MAPPING` / `_resolve_pilot_type()` gated on registry `classifier.status == "validated"`; enforced with early 400-rejection on both `POST /api/v1/upload?type=...` and `POST /api/v1/result/<id>/retry`. Frontend dropdowns expanded from 5 legacy options to the real 11 (they had never exposed more than 5 to begin with).
- **13 collision pairs tested for the first time** ([run_collision_pairs.py](file:///home/stardhoom/LDV/ldv-backend/tests/run_collision_pairs.py)) — baseline A-signal 7/13, B-signal 11/13. Found 5 expected draft→validated collisions, 2 unexplained double-misses (`maintenance_contract`, `outsourcing_agreement` → `employment_contract`), and 1 distinct orphaned-label bug (`it_services_contract`). None are silent-wrong-result risks in the pilot — `worker._needs_confirmation()`'s draft-profile gate already forces human confirmation regardless.

### 07-22
- **`it_services_contract` orphaned label fixed** — hardcoded keyword chain returned a legacy pre-consolidation string (`"it service agreement"`) that never matched the registry's actual display name. Fixed 3 occurrences to `"it services contract"`.
- **`outsourcing_agreement` → `employment_contract` fixed** — plain substring match on `"employee"` false-triggered inside `"employees"` in a title-matching branch. A broad fix (word-boundary-match all 18 branches) regressed a different case and was reverted; scoped to the single `employment` branch instead.
- **`maintenance_contract` → `employment_contract` fixed**, decided without needing the flagged "joint override-logic decision" — root cause was simply a missing title-match branch (same precedented pattern as `construction`/`insurance`), not new override logic. Auditing the chain surfaced 2 more identical orphaned-label bugs (`construction_contract`, `insurance_contract`), fixed alongside it. First attempt at a bare substring match regressed 4 unrelated collision pairs via boilerplate text; rescoped to the registry's exact alias phrase, word-boundary matched.
- **2 fixture-content gaps closed** (`commercial_agreement`, `partnership_agreement`) — both already had an unused `docx/*_pos.docx` variant sitting in the fixtures directory (same content, different container, matching the precedent already accepted for `purchase_agreement`'s 3rd slot) that had never been wired into the harness's `FIXTURE_MAP`. Verified both classify correctly before wiring in.

### 07-23
- **CRA Release Gate Matrix created** ([docs/CRA_RELEASE_GATE_MATRIX.md](file:///home/stardhoom/LDV/docs/CRA_RELEASE_GATE_MATRIX.md)) — single authoritative 5-gate status doc replacing scattered percentages across separate reports. Result at creation: 1/5 passed (Engineering Regression), Gates 2/3 partial, Gates 4/5 not started.
- **45-vs-42 detection-spec reconciliation started** ([docs/detection_spec_reconciliation_45_vs_42.md](file:///home/stardhoom/LDV/docs/detection_spec_reconciliation_45_vs_42.md)) — registry-side audit showed 45/45 draft profiles already have a classifier spec, contradicting the long-standing "42" figure; flagged as needing Ilham's counting method to fully close.
- **Merged `release/cra-1.0-rc1` → `staging`**, surfacing ~270 files / ~20,000 lines of previously-unknown parallel work already on `origin/staging` — see §7 for full detail. One real code conflict (`detector_distilbert.py`), resolved by keeping RC1's already-tested implementation (RC1 had already deleted the dead duplicate `_keyword_doc_type()` staging still carried). Full suite re-verified: 108/108.
- **Fixed a pre-existing staging bug surfaced by the merge**: `_NLI_OVERRIDE_THRESHOLD` was defined twice in `detector_distilbert.py` (0.85, then a dead-code duplicate at 0.40 further down) — Python kept the last one, so the classifier's actual override threshold had silently been 0.40 instead of the intended/tested 0.85. Confirmed via history this predated the merge (already present at staging's pre-merge tip) — not something the merge introduced. Removed the duplicate; suite re-verified 108/108.
- **Fast-forwarded `release/cra-1.0-rc1` → `staging`** (RC1 had zero commits `staging` didn't already have) so both branches point to the same commit going forward.
- **Resolved the 45-vs-42 discrepancy** using Ilham's `docs/GOLD_STANDARD_VALIDATION_SUMMARY.md` (one of the newly-merged files): 42 "Partial Validation Records" + 3 "Profiles Pending Engineering Sync" (`construction_contract`/`insurance_contract`/`it_services_contract`, each held back by a registry-vs-legacy-JSON `required_clauses` mismatch, not a missing spec) = 45. Ilham's own doc confirms 45/45 detection specs present, matching the registry-side audit from earlier the same day. Bonus finding while cross-referencing: Ilham's `ALIAS_REVIEW.md` had already independently closed 1 of the 6 open collision pairs (`joint_venture_agreement` vs `partnership_agreement`) with the same fix Afridho's `classifier_collision_pairs.md` had proposed.
- **Synced `required_clauses` for the 3 pending-engineering-sync profiles**, per Ilham's `REQUIRED_CLAUSE_RECONCILIATION.md`: applied the union of registry and legacy-JSON clause sets to both sides for `construction_contract` (+`insurance`, `limitation_liability` to registry; +`delivery_terms` to JSON), `it_services_contract` (+`confidentiality` to registry; +`ip_ownership`, `warranty_disclaimer` to JSON), and `insurance_contract` (+`payment_terms`, `limitation_liability`, `default_provisions`, `notice_period` to registry; +`insurance` to JSON). All clause IDs verified against `detector_rules._CLAUSE_RULES`; `validate_profiles.py` clean (56 profiles, 43 approved clause IDs, 0 unmapped); full suite 108/108.

---

## 4. Ilham's Action Items

Carried 07-20 through 07-22 without engineering-side progress possible; status as of 07-23 below:

1. Expected-results matrix for the 33 corpus fixtures (risk score, dangerous-clause severity, recommendation wording) — **still open**. Ilham's newly-merged `docs/lightml/corpus_expected_results.md` has values for all 33, but they're a repeated 3-tier template (fixed score bands, fixed red flags per tier), not individually-verified per-fixture ground truth — doesn't yet satisfy this item.
2. ~~Reconciliation of the 45-partial-profiles vs. 42-detection-specifications discrepancy~~ — **✅ CLOSED 07-23**, using Ilham's `GOLD_STANDARD_VALIDATION_SUMMARY.md`. See §3 (07-23) and §7.
3. Profile-by-profile legal validation matrix with named reviewer, review date, and approval status per profile — **still open**. Ilham's merged docs (`docs/lightml/legal_profile_evidence/*_LEGAL_EVIDENCE.md`, `GOLD_STANDARD_TRACEABILITY.md`) have real per-clause statutory citations for the 11 mature profiles, but reviewer/approval fields are uniformly `Pending`/blank across all 56 — 0/56 formally approved, confirmed independently by both sides now.
4. Joint CRA Release Gate Matrix (5 gates) — **✅ CREATED 07-23** (`docs/CRA_RELEASE_GATE_MATRIX.md`), by engineering. Not yet reviewed/co-signed by Ilham.
5. Judgment call on the 5 confirmed keyword-duplication collisions (surfaced 07-21) — **1/5 closed** (`joint_venture_agreement` vs `partnership_agreement`, via Ilham's `ALIAS_REVIEW.md`); **4 still open**: `maintenance_contract`/`service_agreement`, `licensing_agreement`/`software_license`, `outsourcing_agreement`/`service_agreement`, `banking_facility_agreement`/`loan_agreement`. (`employment_termination_agreement`/`employment_contract` also remains open — 5 total per the gate matrix's current count.)

---

## 5. End-of-Period State (2026-07-23 EOD)

No open engineering items requiring code changes from this thread beyond what's listed above. All 6 items of the 07-17 punch list closed (07-21), all 4 follow-ups from that closure closed (07-22), the 45-vs-42 discrepancy and its clause-sync follow-up closed (07-23). Remaining blockers are entirely non-engineering: legal sign-off (Gate 4), 5 collision-pair keyword-ownership decisions, risk-score ground truth review, and the `saas_agreement` registry gap — tracked in §4 above and `docs/CRA_RELEASE_GATE_MATRIX.md`.

---

## 6. Sign-off

- **Engineering Owner:** Afridho (Engineering Owner)
- **Status:** SUBMITTED FOR REVIEW
- **Period:** 2026-07-20 through 2026-07-23

---

## 7. Merge Status — Ilham's Parallel Work (New 07-23)

**Discovery:** while merging `release/cra-1.0-rc1` into `staging`, `origin/staging` turned out to already contain **~270 files / ~20,000 lines** of documentation and data that hadn't been visible from RC1 or any prior report — file paths reference `C:\Users\ADVAN\cra`, a different machine, consistent with this being Ilham's own engineering-adjacent output rather than a legal-only deliverable.

**What's in it, by category:**
- `docs/lightml/legal_profile_evidence/*_LEGAL_EVIDENCE.md` (11 files, mature profiles only) — real statutory citations per required clause (KUH Perdata articles, French Code civil, UU 13/2003, etc.); the best-developed new content.
- `docs/GOLD_STANDARD_VALIDATION_SUMMARY.md` / `GOLD_STANDARD_TRACEABILITY.md` (2,080 lines) — the most rigorous new artifact; source of the 45-vs-42 resolution (§3, §7 below).
- `docs/DATASET_QA_REPORT.md`, `LEGAL_TERMINOLOGY_REVIEW.md`, `LEGAL_CITATION_VERIFICATION.md`, `ALIAS_REVIEW.md` — specific findings: dataset duplication in `dangerous_clauses_MASTERv2.csv`, FR/NL aliases flagged "machine-translated, do not use in production," and the collision-pair fix noted above.
- `docs/lightml/detection_specifications/*.md` (45 files) and `docs/lightml/contract_corpus/*/*.md` (168 files) — largely templated/thin (most fields literally `"Insufficient source material"` or `"Pending"`), not yet real content.
- `docs/lightml/corpus_expected_results.md` — filled in but templated (see §4 item 1 above).
- 8 `.xlsx` workbooks (legal validation matrix, traceability matrix, coverage report, etc.) — catalogued, not yet parsed/reviewed.
- `docs/REQUIRED_CLAUSE_RECONCILIATION.md` — the doc that directly enabled today's `required_clauses` sync fix (§3).

**Confirmed via Ilham's own new docs (independent of anything Afridho previously reported):**
- Legal review has not started for any profile — `GOLD_STANDARD_VALIDATION_SUMMARY.md` reports "Profiles Pending Legal Review: 52/56," and even downgrades the 11 mature profiles' status language from "Ready for production" to "Engineering Readiness Verified; Legal Approval Pending."
- The 45-vs-42 figure is explained (see §3), though one internal inconsistency remains in Ilham's own doc set: some of the newly-merged files still report "45/45 detection specs, 100% compliant" using the flat figure, while `GOLD_STANDARD_VALIDATION_SUMMARY.md` uses the narrower 42+3 split — both are now understood to be consistent (42+3=45), but the two framings weren't cross-referenced against each other in the source material.

**Bug found and fixed during the merge** (§3, 07-23): dead duplicate `_NLI_OVERRIDE_THRESHOLD` definition in `detector_distilbert.py`, predating the merge on staging's side. Fixed, verified, pushed.

**Outcome:** `release/cra-1.0-rc1` and `staging` are now fast-forward-identical (as of `2d37cbc`), both pushed to origin. Two-way information flow is established for the first time this week — engineering's gate matrix now incorporates Ilham's legal-evidence and validation-dataset work, and two engineering items (45v42, clause sync) got closed using his data same-day.
